_schema-version: 3.3.0
ID: beCash
description: A simple CAP project.
version: 1.0.0
modules:
- name: beCash-srv
  type: nodejs
  path: gen/srv
  requires:
  - name: beCash-db
  provides:
  - name: srv-api
    properties:
      srv-url: ${default-url}
  parameters:
    buildpack: nodejs_buildpack
    instances: 1
  build-parameters:
    builder: npm-ci
- name: beCash-db-deployer
  type: hdb
  path: gen/db
  requires:
  - name: beCash-db
  parameters:
    buildpack: nodejs_buildpack
- name: beCash-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: beCash-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - comnbxmanagerapp.zip
      name: comnbxmanagerapp
      target-path: resources/
    - artifacts:
      - comnbxemployeeapp.zip
      name: comnbxemployeeapp
      target-path: resources/
    - artifacts:
      - comnbxadminapp.zip
      name: comnbxadminapp
      target-path: resources/
- name: comnbxmanagerapp
  type: html5
  path: app/manager_app
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
- name: comnbxemployeeapp
  type: html5
  path: app/employee_app
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
- name: comnbxadminapp
  type: html5
  path: app/admin_app
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: beCash-db
  type: com.sap.xs.hdi-container
  parameters:
    service: hana
    service-plan: hdi-shared
- name: beCash-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: beCash-html5-service
    service-plan: app-host
- name: beCash-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: false
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: beCash-destination-service
    service-plan: lite
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
build-parameters:
  before-all:
  - builder: custom
    commands:
    - npm ci
    - npx cds build --production
